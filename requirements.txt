# Local development: everything at once.
# The container images use the split files so a slow download does not
# invalidate an entire layer. See infra/Dockerfile.agent.
-r requirements-core.txt
-r requirements-agent.txt
-r requirements-api.txt
playwright==1.47.0